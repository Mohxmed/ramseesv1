"use client";

/** Transient "saved" confirmation pill. */
export function SAVED_PILL() {
  return (
    <div className="mt-3 inline-flex items-center gap-1.5 rounded-md border border-up/40 bg-up/10 px-2.5 py-1 text-xs font-semibold text-up-fg">
      ✓ تم حفظ الاستراتيجية
    </div>
  );
}
